// config.js — dimensiuni tablă + constante de joc

export const boardWidth = Math.min(window.innerWidth - 40, 720);
export const boardHeight = Math.round(boardWidth * 0.65);

export const simboluriPerCard = 7;
